Currently we only test to make sure we don't get any overflows or packet RX errors when the internal CLK clock is running faster than the RX_CLK. 


WE DO NOT TEST for errors when RX_CLK is faster than CLK. 
